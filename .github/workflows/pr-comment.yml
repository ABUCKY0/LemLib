name: Add Template to Pull Request 

on:
  workflow_run:
    workflows: ['PROS Build Template']
    types: [completed]

  workflow_dispatch: 

jobs:
  pr-comment:
    uses: abucky0/lemlib-common-files-test/.github/workflows/pr-comment.yml@main
